<fieldset class="text-input {{className}} {{#if error}}text-input_state_error{{/if}}">
  <div class="text-input__wrapper">
    <label for="{{id}}" class="text-input__label">
      {{label}}
    </label>
    <input
      type="{{type}}"
      name="{{name}}"
      id="{{id}}"
      class="text-input__input"
      value="{{value}}"
    />
  </div>

  {{#if error}}
    <p class="text-input__error">{{error}}</p>
  {{/if}}
</fieldset>